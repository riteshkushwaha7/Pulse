# 🚀 Pulse - Mini Social Post App Frontend

Production-ready React frontend for Pulse social feed.

## Setup

1. `npm install`
2. Copy `.env.example` to `.env` and set your backend API URL
3. `npm start`

## Features
- Auth (Signup/Login)
- Feed with posts, likes, comments
- Create post (text or image)
- Responsive, modern UI (Material UI)
- Pagination (Load More)

## Deploy
- Ready for Vercel/Netlify
